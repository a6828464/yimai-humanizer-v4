---
name: yimai-humanizer-v4
description: "Use when detecting or removing AI味 from Chinese articles, WeChat posts, business copy, reports, or fiction. Supports detect, light-edit, and rewrite modes while preserving facts and author voice."
version: 4.0.0
license: MIT
metadata:
  hermes:
    tags: [writing, humanizer, chinese, wechat, editing, anti-ai-slop]
    category: creative
    sources:
      - petergyang/no-ai-slop
      - masterball-w/Master-humanizer-skill
      - unclecheng-reduce-ai-perception-v2
---

# 一麦中文 Humanizer v4

把中文文本改得更具体、自然、有作者声音。目标是编辑质量，不是欺骗检测器，也不判断文本是否一定由 AI 生成。

## 最高优先级

1. **事实安全**：不得新增或改动原文没有的数字、案例、地点、经历、引语、产品功能、用户反馈、感官细节和观点。缺素材就保留概括、标注缺口或询问用户。
2. **保留作者**：先识别作者的词汇、节奏、直接程度、幽默、犹豫、情绪和合理跑题。只修改有证据的问题句。
3. **最小有效修改**：能删一个空词就不重写一句；能改一句就不重写一段。强句不为统一风格而修改。
4. **模式优先**：词表只是线索。一个词只有在承担空洞、套路或伪口语功能时才处理，不做机械清零。
5. **按场景处理**：人类原稿默认轻改；纯 AI 初稿或用户明确要求重写时才使用两遍法。
6. **信息与声音分开**：删除信息冗余，保留有声音、情绪、节奏或叙事作用的冗余。

## 三种模式

### detect

触发：用户要求检测、审稿、找 AI 味，但不要求改写。

输出每个问题的：
- 模式名称
- 原文短引
- 为什么有问题
- 一句修改方向

不重写全文，不打 AI 概率分，不断言作者身份。

### light-edit（默认）

适用于人类草稿、经营文案、报告、既有公众号文章。

- 做最小有效修改。
- 保留结构，除非结构妨碍理解或明显模板化。
- 不主动添加口头禅、自嘲、极短句、跑题或感官细节。
- 返回完整修改稿和简短“改了什么”。

### rewrite

仅用于纯 AI 初稿、重度模板稿，或用户明确要求重写。

采用两遍法：
1. 第一遍识别原稿实际想表达的事实、观点、受众和行动目标。
2. 第二遍脱离原句式重新表达，不做机械同义词替换。
3. 重写仍受事实安全约束；没有素材就不造细节。

## 处理流程

1. 确认任务目标、受众、发布场景和模式；信息足够时直接执行。
2. 提取不可变内容：事实、数字、专名、引语、立场、行动要求。
3. 记录3至5个作者声音信号，只在内部使用。
4. 按 `references/patterns.md` 扫描，按影响排序，不按词表计数定罪。
5. 按模式编辑；遇到事实缺口不得用“更具体”为理由编造。
6. 按 `references/scenarios.md` 做场景适配。
7. 用 `references/eval.md` 自检；失败则修复一次，最多三轮。
8. 输出用户需要的结果，不默认暴露冗长质检过程。

## 规则优先级

发生冲突时依次服从：

1. 事实与引用准确
2. 原意与作者声音
3. 场景和读者需要
4. 清晰、具体、可读
5. 去 AI 模式
6. 标点、词频、节奏等软偏好

因此：
- 冒号、破折号、引号不是绝对禁用，只处理装饰性滥用。
- “不是A，而是B”不是字符禁令；只有作为虚假戏剧性模板时才改。
- 成语不设机械配额，以自然、准确、不过密为准。
- 短句不是越多越真人；残缺短句和连续口号式碎句应合并。
- 结构不必故意打乱；只移除无作用的小标题、递进和重复总结。
- 不为了“活人感”主动塞入“说真的、怎么说呢、不是哥们、你敢信”等口头禅。

## 必须避免

- 把“六小时”改成“两小时”以增加冲击力。
- 凭空增加地址、营业年限、功能、案例数量或用户反馈。
- 把抽象情绪自动改成虚构动作，如原文未提供“手在抖”就不能添加。
- 将所有比喻、第一人称、犹豫词或破折号一律删除。
- 把作者的粗糙个性修成统一的标准公众号腔。
- 为规避 AI 检测器故意制造错别字、逻辑断裂或随机词汇。

## 输出格式

### detect

```markdown
## 检测结果

1. **模式名称**
   - 原文：……
   - 问题：……
   - 建议：……
```

### light-edit / rewrite

```markdown
## 修改稿

……

## 改了什么

- ……
- ……
```

只有用户明确要求时才附完整质检报告或逐句对照。

## 来源吸收说明

- 从 `no-ai-slop` 吸收：最小有效修改、保留作者声音、检测与编辑分离、事实保护、独立验收。
- 从 `Master-humanizer-skill` 吸收：两遍法、模式优先、一判定名、中文伪口语、案例模板和段落服务题旨。
- 从经营智能体旧版吸收：中文小说/情感/营销模式、轻中重处理思想、三轮停止条件和分层自检。
- 已删除旧版中的绝对禁词、绝对标点禁令、强制口语注入，以及会诱发事实编造的示例规则。
